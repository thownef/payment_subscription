export enum DeliveryType {
  IMMEDIATE = 'immediate',
  SCHEDULED = 'scheduled'
}

export enum DeliveryStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  FAILED = 'failed'
}

export enum TemplateType {
  TEMPLATE = 'template',
  DRAFT = 'draft',
  DESIGN = 'design'
}
